<%= link_to event.date, event_path %>
<table>
  <thead>
    <tr>  <% @events.each do |event| %>
      <th> <%= event.track %></th>
    </tr>
    <% end %>
  </thead>
  <tbody>
    <% @events.each do |event| %>
    <tr>
      <td> <%= event.start %></td>
      <td> <%= event.finish %></td>
      <td> <%= event.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>


<div>

<h3>Import a CSV File</h3>
  <%= form_tag import_events_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag "Import CSV" %>
  <% end %>
</div>
